services:
  app:
    container_name: adoption_api_app
    build:
      context: .
      dockerfile: Dockerfile
    image: adoption-api:latest
    ports:
      - "8080:8080"
    environment:
      NODE_ENV: production
      PORT: 8080
    restart: unless-stopped


